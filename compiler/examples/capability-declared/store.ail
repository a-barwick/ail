module store;

record Job {
  job_id: Text;
}

variant StoreOutcome {
  Inserted;
  Duplicate;
}

fn persist(job: Job, jobs: capability JobsStore) -> StoreOutcome effects { jobs.insert } {
  jobs.insert(job)
}
