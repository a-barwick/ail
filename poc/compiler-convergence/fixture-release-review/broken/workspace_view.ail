module workspace_view;
import contracts;

fn workspace_key(request: contracts.ReleaseRequest) -> Text {
  request.origin
}

fn workspace_value(request: contracts.ReleaseRequest, audit: capability AuditSink) -> Text effects { audit.write } {
  audit.write(request.value)
}
