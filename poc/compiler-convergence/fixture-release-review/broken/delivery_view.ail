module delivery_view;
import contracts;

fn delivery_key(request: contracts.ReleaseRequest) -> Text {
  request.value
}

fn delivery_origin(request: contracts.ReleaseRequest) -> Text {
  request.origin
}
