module batch;
import classify;
import types;

fn classify_batch(requests: List<types.LabelRequest, 8>) -> List<types.Label, 4> effects { journal.write } {
  map request in requests {
    classify.classify_one(request)
  }
}
